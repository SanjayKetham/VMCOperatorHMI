version: '3.8'

services:
  vmc-hmi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - sqlite_data:/app/db
    restart: always

volumes:
  sqlite_data:
